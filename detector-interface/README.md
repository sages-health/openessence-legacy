This module defines the OpenEssence detector API. Detector implementations, such as OpenEssence's own `open-detectors`,
can use this API to implement new detectors that can easily be added to OpenEssence.

